"use strict";
exports.__esModule = true;
function defaultComparetor(l, r) {
    return l > r
        ? 1
        : l < r
            ? -1
            : 0;
}
exports.defaultComparetor = defaultComparetor;
